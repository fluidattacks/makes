jobs:
  formatBash:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: fluidattacks/makes@main
      with:
        args: .formatBash
name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
